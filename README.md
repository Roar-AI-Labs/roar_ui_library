# Roar Workspace UI monorepo

- **`docs/`** — Static design system reference (open `docs/index.html` or run `npx --yes serve docs`).
- **`packages/ui/`** — React library **`@roar-workspace/ui`** (`npm run build` from the repo root).

See **[ROADMAP.md](ROADMAP.md)** for phased delivery (Phase 0: repository shape).
